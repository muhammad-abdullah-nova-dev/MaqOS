# MaqOS
A simulated desktop operating system built from scratch in C++17 &amp; SFML. Implements real OS internals including process creation (fork/exec), a 3-level Multilevel Queue scheduler, pipeline IPC handshakes, deadlock detection via DFS on resource graphs, memory semaphores, and integration with the host Linux /proc filesystem.
